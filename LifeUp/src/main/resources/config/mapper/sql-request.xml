<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd"> 

<mapper namespace="common">

	<!-- 견적의뢰 등록-->
	<insert id="setReg" parameterType="salesman.model.EstimateRequestVo">
	insert into salesman.estimate_request (	  customer_id,	region_cd,car_id,car_model,car_option,customer_req,	status,create_date  ) 
	values (  #{customer_id},#{region_cd},#{car_id},#{car_model},#{car_option},#{customer_req},#{status},#{create_date}  )
	</insert>
	
	<!-- 지역 시 를  가지고 온다  -->
	<select id="setSido" resultType="java.util.HashMap">
	   select SIDO from region
 	   group by sido
	   order by REGION_CD asc
	</select>

</mapper>